export const template = (t) => `
    <header>
        <h1>${t.title}</h1>
    </header>
    <main>
        <article>
            <p>${t.description}</p>
            <p>${t.goal}</p>
        </article>
    </main>
`; 

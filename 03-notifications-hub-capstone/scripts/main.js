/**
 * main.js — entry point for lesson 03: Notifications Hub Capstone.
 *
 * Imports the two custom elements so the browser registers them
 * before the parser upgrades the elements in index.html.
 */

import '../components/status-publisher/status-publisher.js';
import '../components/notifications-hub/notifications-hub.js';
